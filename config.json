{
    "usage": [
        "The following variables can be used:",
        "  $script_dir    - the directory from wich the script was run",
        "  $config_dir    - the directory where the config file is located",
        "",
        "A kit can be referenced as:",
        "  kit:<name>",
        "so this will add all packages contained in the referenced kit"
    ],
    "install_dir": "C:\\Dev\\Tools",
    "packages_dir": "$config_dir\\packages",
    "cache_dir": "$config_dir\\cache",
    "user_name": "build.bot",
    "user_info": "Build Bot <build.bot@example.org>",
    "kits": {
        "default": [
            "kit:config", "kit:common"],
        "config": [
            "Env_config"],
        "common": [
            "Python__3", "Conan", "Conan_config", "TortoiseHg", "TortoiseHg_config",
            "Git", "Git_config"],
        "qt": [
            "Qt_Creator", "Qt_Creator_ext_CDB"]
    }
}
